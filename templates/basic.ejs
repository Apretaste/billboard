<div class="row">
	<div class="col s12">
		<h1>Billboard</h1>
		<p>Las 100 canciones más escuchadas en EU. Como muchas son recientes, puede que aún no tengan letra.</p>
	</div>
</div>

<div class="row">
	<% for(var i=0; i < tracks.length; i++) {
		var track = tracks[i]; %>
		<div class="col s12 m6 xl4">
			<div class="card light-green lighten-5">
				<div class="card-content song-card">
					<span class="card-title"><%= track.song_title %></span>
					<span class="badge"><%= i + 1 %></span>
					<p class="blue-grey-text">Por <%= track.artist %></p>
				</div>
				<div class="card-action blue-text">
					<% if (track.link) { %>
						<a class="blue-text" href="#!" onclick="apretaste.send({command: 'BILLBOARD LETRA', data: {query: tracks[<%= i %>].link}}); %>">Letra</a>
					<% } else { %>
						<br/>
					<% } %>
				</div>
			</div>
		</div>
	<% } %>
</div>