<div class="row">
	<div class="col s12">
		<h1>Billboard</h1>
		<p>Las 100 canciones más escuchadas en los Estados Unidos. Como muchas son recientes, puede que aún no tengan letra.</p>
	</div>
</div>

<div class="row">
	<% for(var i=0; i < tracks.length; i++) { %>
		<div class="col s12 m6 xl4">
			<div class="card light-green lighten-5">
				<div class="card-content song-card">
					<span class="card-title"><%= tracks[i].title %></span>
					<span class="badge"><%= i + 1 %></span>
					<p class="blue-grey-text">Por <%= tracks[i].artist %></p>
				</div>
				<div class="card-action blue-text">
					<a class="blue-text" href="#!" onclick="apretaste.send({command:'LETRAS SEARCH', data:{query:'<%= tracks[i].title %>'}})">Letra</a>
				</div>
			</div>
		</div>
	<% } %>
</div>