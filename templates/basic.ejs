<div class="row">
	<div class="col s12">
	<h1>100 canciones m&aacute;s populares</h1>
	</div>
</div>

<div class="row">
	<div class="col s12">
		<p>Las 100 canciones m&aacute;s escuchadas en EU. Como muchas son recientes, puede que a&uacute;n no tengan letra.</p>
	</div>
</div>

<div class="row">
	<% for(var i=0; i < tracks.length; i++) {
		var track = tracks[i]; %>
			<div class="col s12 l4 m6 xl4">
				<div class="card grey lighten-5 z-depth-1">
					<div class="card-content song-card">
						<span class="card-title"><%= track.song_title %></span>
						<p>by <%= track.artist %></p>
					</div>
					<div class="card-action">
						<% if (track.link) { %>
							<a href="#!" onclick="apretaste.send({command: 'BILLBOARD LETRA', data: {query: tracks[<%= i %>].link}}); %>"><small>Letra</small></a>
						<% } else { %>
							<br/>
						<% } %>
					</div>
				</div>
			</div>
	<% } %>
</div>